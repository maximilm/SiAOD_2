#pragma once
#include <iostream>
#include <iomanip>
#include <ctime>
#include <fstream>
#include "Polis.h"
using namespace std;

	void genText(int n);
	string gen_random_dig(const int len);
	string gen_random_b(const int len);

